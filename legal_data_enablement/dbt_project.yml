name: 'legal_data_enablement'
version: '1.0.0'
profile: 'legal_data_enablement'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  legal_data_enablement:
    staging:
      +schema: staging
    picklists:
      +schema: staging
    gisds:
      +schema: staging
    curated:
      +schema: curated
